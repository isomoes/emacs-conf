;;; general.el --- Leader-key definer (general.el) -*- lexical-binding: t; -*-

;;; Commentary:
;;
;; Installs `general', the leader-key definer.  The actual bindings — the `SPC'
;; leader map and the global `override' keys — live in keymaps.el, loaded at the
;; very end of init.el (after this file, so the definer exists).  Loaded by init.el.

;;; Code:

(use-package general)

;;; general.el ends here
